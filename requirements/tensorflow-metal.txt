# Optional: GPU acceleration on Apple Silicon (macOS, arm64) via Metal.
#
# Use INSTEAD OF requirements/tensorflow.txt on Apple-Silicon Macs.
# tensorflow-metal is a PluggableDevice hard-pinned to a narrow TF version
# window: 1.2.0 (the newest) supports TensorFlow ~2.16, NOT 2.17+.
#
# The pretrained WINMOL .hdf5 models are Keras 2 artifacts and fail to load
# under the Keras 3 that ships with TF 2.16 (float Dropout seed). tf-keras is
# the legacy Keras 2 shim; activate it by exporting TF_USE_LEGACY_KERAS=1
# before TensorFlow is imported (the standalone entrypoint sets this itself).
tensorflow==2.16.2
tensorflow-metal==1.2.0
tf-keras==2.16.0
