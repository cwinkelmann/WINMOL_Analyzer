# Shared geo/science stack, runtime-free: names NO onnxruntime variant.
# Include from cpu.txt (or a future gpu.txt) — never install alone.
Pillow==10.1.0
Shapely==2.0.2
geopandas==0.14.0
numpy==1.26.4
rasterio>=1.4,<1.5
scikit-image==0.22.0
scipy==1.11.3
pyogrio>=0.9

# The ONNX *authoring* library, not the runtime (that is onnxruntime, and
# it lives in cpu.txt/gpu.txt because the two variants must never mix).
# utils/onnx_preprocess.py imports it to prepend the normalize + Resize
# nodes that make prediction_read_strategy=graph -- THE DEFAULT since
# 6ff1da2 -- v0.5-equivalent. Before that default flipped nothing in the
# runtime imported it, which is how it reached users as a
# ModuleNotFoundError at model load instead of a missing requirement.
# Unpinned so it can track protobuf; the numpy pin above is the guard --
# an onnx release that demands numpy 2 fails the resolve loudly rather
# than silently swapping the ABI the geo stack is built against.
onnx>=1.15
