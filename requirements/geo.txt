# The geo/raster stack the compute pipeline actually imports — NO inference
# runtime.
#
# Split out of base.txt so that CPU and GPU environments can share it: pip
# installs of `onnxruntime` and `onnxruntime-gpu` both provide the same
# `onnxruntime` module and must never be co-installed, so a GPU requirements
# file cannot simply `-r base.txt`.
#
#   base.txt = geo.txt + onnxruntime      (CPU / plugin / CI)
#   gpu.txt  = geo.txt + onnxruntime-gpu  (CUDA containers)
#
# Versions are pinned because the golden fixtures are only bit-reproducible
# under this set (see ci.txt).
Pillow==10.1.0
Shapely==2.0.2
geopandas==0.14.0
rasterio>=1.4,<1.5
scikit-image==0.22.0
scipy==1.11.3
pyogrio>=0.9
